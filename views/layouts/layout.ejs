<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EZRETRO</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Importing Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">

    <!-- Custom CSS -->
    <style>
        body {
            font-family: 'Press Start 2P', cursive; /* Retro-style font for a gaming vibe */
            background-color: #121212; /* Dark background for contrast */
            color: #e0e0e0; /* Light text color for readability */
        }

        .navbar {
            background-image: linear-gradient(45deg, #0d6efd, #6610f2);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .navbar-brand {
            font-size: 1.5rem;
            letter-spacing: 1px;
            color: #fff !important;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
        }

        .nav-link {
            color: #f8f9fa !important;
            transition: color 0.3s ease;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
        }

        .nav-link:hover {
            color: #ffc107 !important; /* Highlight link on hover */
        }

        .btn-outline-warning, .btn-outline-success, .btn-outline-danger {
            border-radius: 20px;
            border-width: 2px;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
        }

        .btn-outline-warning:hover {
            background-color: #ffc107;
            color: #000;
        }

        .btn-outline-success:hover {
            background-color: #28a745;
            color: #fff;
        }

        .btn-outline-danger:hover {
            background-color: #dc3545;
            color: #fff;
        }

        .form-control {
            border-radius: 30px;
            background-color: #343a40;
            color: #fff;
            border: 1px solid #495057;
            transition: border-color 0.3s ease;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
        }

        .form-control:focus {
            border-color: #0d6efd;
            box-shadow: none;
        }

        .btn-primary {
            border-radius: 20px;
            background-image: linear-gradient(45deg, #6610f2, #0d6efd);
            border: none;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
        }

        .btn-primary:hover {
            background-image: linear-gradient(45deg, #0d6efd, #6610f2);
            color: #fff;
        }

        /* Custom styles for the main content area */
        .content {
            margin-top: 50px;
        }

        /* Retro hover animation for navbar links */
        .nav-link {
            position: relative;
            overflow: hidden;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 2px;
            background-color: #ffc107;
            bottom: 0;
            left: -100%;
            transition: left 0.3s ease;
        }

        .nav-link:hover::after {
            left: 0;
        }

        /* Search Bar Styles */
        .search-container {
            position: relative;
            display: flex;
            align-items: center;
        }

        .search-input {
            width: 0;
            padding: 0;
            height: 36px;
            border: none;
            border-radius: 20px;
            background-color: transparent;
            transition: width 0.4s ease, padding 0.4s ease, border-color 0.4s ease;
            font-family: 'Press Start 2P', cursive; /* Apply retro font */
            outline: none;
            color: #fff;
            font-size: 0.9rem;
            border: 1px solid transparent;
        }

        .search-container.active .search-input {
            width: 220px; /* Expanded width */
            padding: 5px 10px;
            border-color: #0d6efd;
            background-color: #343a40;
        }

        .search-icon {
            width: 24px;
            height: 24px;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .search-container.active .search-icon {
            transform: rotate(90deg);
        }

        .close-icon {
            width: 24px;
            height: 24px;
            cursor: pointer;
            transition: opacity 0.3s ease;
            opacity: 0;
            position: absolute;
            right: 5px;
        }

        .search-container.active .close-icon {
            opacity: 1;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .navbar-brand {
                font-size: 1.2rem;
            }

            .btn {
                font-size: 0.8rem;
            }

            .search-input {
                width: 150px; /* Adjust for smaller screens */
            }
        }
    </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">EZRETRO</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <% if (req.isAuthenticated()) {%>
              <a class="nav-link" href="/profile/<%= req.user._id %>">Status</a>
            <% } %>
          </li>
          <li class="nav-item">
              <a class="nav-link" href="/plans">Subscribe</a>
          </li>
        </ul>
        <div class="d-flex align-items-center">
          <% if (req.isAuthenticated()) {%>
            <a class="btn btn-outline-danger me-3" type="button" id="logoutBtn" href="/logout">Logout</a>
          <% } else { %>
            <a class="btn btn-outline-warning me-2" type="button" id="registerBtn" href="/register">Register</a>
            <a class="btn btn-outline-success me-3" type="button" id="loginBtn" href="/login">Login</a>
          <% } %>
  
          <!-- Animated Search Bar -->
          <div class="search-container" id="searchContainer">
            <img src="/assets/search-icon.png" alt="Search" class="search-icon" id="searchIcon">
            <input type="text" class="search-input" id="searchInput" placeholder="Search..." aria-label="Search" value="<%= req.query.searchTags %>" name="searchTags">
            <img src="/assets/close-icon.png" alt="Close" class="close-icon" id="closeIcon">
          </div>
        </div>
      </div>
    </div>
  </nav>
  

<!-- Body -->
<%- body %>

<!-- Bootstrap JS and dependencies -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>

<!-- JavaScript for Search Bar Animation -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const searchContainer = document.getElementById('searchContainer');
    const searchIcon = document.getElementById('searchIcon');
    const searchInput = document.getElementById('searchInput');
    const closeIcon = document.getElementById('closeIcon');

    searchIcon.addEventListener('click', function() {
      searchContainer.classList.toggle('active');
      if (searchContainer.classList.contains('active')) {
        searchInput.focus();
      }
    });

    closeIcon.addEventListener('click', function() {
      searchContainer.classList.remove('active');
      searchInput.value = '';
    });
  });
</script>

</body>
</html>