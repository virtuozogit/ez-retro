<div class="container mt-5">
    <h2 class="text-center mb-4">Subscribe to Your Gaming Adventure</h2>
    <form action="/subscribe" method="POST">
        <div class="form-row mb-3">
            <div class="form-group col-md-6 mb-4">
                <label for="subscriptionPlan" class="form-label">Subscription Plan</label>
                <select class="form-control form-control-lg" id="subscriptionPlan" required>
                    <option value="standard">Standard</option>
                    <option value="premium">Premium</option>
                </select>
            </div>
            <div class="form-group col-md-6 mb-4">
                <label for="creditCardNumber" class="form-label">Credit Card Number</label>
                <input type="text" class="form-control form-control-lg" id="creditCardNumber" placeholder="Enter credit card number" required>
            </div>
        </div>
        <div class="form-row mb-3">
            <div class="form-group col-md-6 mb-4">
                <label for="phoneNumber" class="form-label">Phone Number</label>
                <input type="tel" class="form-control form-control-lg" id="phoneNumber" placeholder="Enter phone number" required>
            </div>
            <div class="form-group col-md-6 mb-4">
                <label for="address" class="form-label">Address</label>
                <input type="text" class="form-control form-control-lg" id="address" placeholder="Enter address" required>
            </div>
        </div>
        <button type="submit" class="btn btn-lg btn-primary w-100">Subscribe Now</button>
    </form>
</div>

<!-- Ensure Bootstrap CSS is included -->
<link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

<style>
    /* Overall Container Styling */
    .container {
        max-width: 800px;
        margin: auto;
        background-color: #f9f9f9;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Form Styling */
    .form-control {
        height: 50px;
        font-size: 1rem;
    }

    .form-control:focus {
        box-shadow: none;
        border-color: #661d15;
    }

    /* Form Label Styling */
    .form-label {
        font-weight: 500;
        color: #333;
    }

    /* Heading Styling */
    h2 {
        font-weight: 700;
        color: #333;
        margin-bottom: 20px;
    }

    /* Button Styling */
    .btn-primary {
        background-color: #661d15;
        border-color: #661d15;
        transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
        background-color: #90291f;
        border-color: #90291f;
    }

    /* Spacing Between Form Rows */
    .form-row {
        margin-bottom: 20px;
    }
</style>
